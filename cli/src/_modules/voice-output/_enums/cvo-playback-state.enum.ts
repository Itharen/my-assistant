/**
 * CVO Playback State Enum
 * @author AI
 * @description Audio playback states for voice output
 */
export enum CVO_PlaybackState {
  idle = 'idle',
  playing = 'playing',
  paused = 'paused',
  stopped = 'stopped',
  buffering = 'buffering',
  error = 'error'
} 
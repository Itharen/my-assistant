// Stable localStorage / sessionStorage keys.
/** localStorage / sessionStorage kulcsok taxonómiája az egész app-ban. */
export enum A_StorageKey {
  authToken = 'ma.auth-token',
  serverBaseUrl = 'ma.server-base-url',
}

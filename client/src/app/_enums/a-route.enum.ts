// Application route paths. Single source of truth for navigation.
/** Az alkalmazás route-jainak taxonómiája — egyetlen forrás a navigációs path-okhoz. */
export enum A_Route {
  dashboard = 'dashboard',
  status = 'status',
  integrations = 'integrations',
  /** FR #3g Reports panel (cycle 96, AGB-24). */
  reports = 'reports',
  // Future: actions = 'actions', userInput = 'user-input', activity = 'activity'
}

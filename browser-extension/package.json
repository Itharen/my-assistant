{
  "name": "@my-assistant/chrome-companion",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Vendor-neutral Chrome Side Panel companion for My Assistant.",
  "scripts": {
    "clean": "rimraf dist",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test dist/background.test.js dist/manifest-contract.test.js"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "rimraf": "^5.0.10",
    "typescript": "^5.5.4"
  }
}
